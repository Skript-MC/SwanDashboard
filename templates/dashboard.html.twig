{% extends 'layouts/panel.html.twig' %}

{% block title %}Tableau de bord{% endblock %}
{% block content %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-3 col-md-6 mb-4">
                <div class="card border-left-primary shadow h-100 py-2">
                    <div class="card-body">
                        <div class="row no-gutters align-items-center">
                            <div class="col mr-2">
                                <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                    Membres Discord
                                </div>
                                <div class="h5 mb-0 font-weight-bold text-gray-800">{{ discordMembers }}</div>
                            </div>
                            <div class="col-auto">
                                <i class="fas fa-users fa-2x text-gray-300"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {% if not is_granted('ROLE_MEMBER') %}
            <div class="col-lg-6 mb-4">
                <div class="card shadow mb-4">
                    <div class="card-header py-3">
                        <h6 class="m-0 font-weight-bold text-primary">Demande d'accès</h6>
                    </div>
                    <div class="card-body">
                        Bienvenue sur le panel de gestion de Swan. Afin d'accéder aux différentes fonctionnalités du panel, il vous suffit de remplir les conditions ci-dessous. Vous aurez ensuite accès aux différents espaces qui s'afficheront dans le menu.
                    </div>
                </div>
            </div>
        {% endif %}
    </div>
{% endblock %}
